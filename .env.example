# Google Cloud OAuth2 (same credentials as PMI project)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=

# Google Drive
INTAKE_FOLDER_ID=                  # teacher's lessons folder ID

# Shared secret — Cloudflare Worker → ENG1, and ENG1 → ENG2 (same value for both)
N8N_SHARED_SECRET=

# n8n
N8N_WEBHOOK_URL=                   # ENG1 production webhook URL

# Supabase (from dashboard → Settings → API)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=         # use service_role, not anon

# OpenAI
OPENAI_API_KEY=
